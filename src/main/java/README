параметры для запуска указывать в файле inputJSON
FirstPort - первый порт в диапазоне
LastPort - последний порт в диапазоне
isUdpSearch - если true, то ищем только udp иначе - только tcp

Запуск из коммандной строки

0)переходим в каталог с файлами
1)в коммандной строке прописываем

javac Main.java
java -classpath . Main

Не уверен, возможно заругается Maven, лучше поэтому все таки через Idea запускать